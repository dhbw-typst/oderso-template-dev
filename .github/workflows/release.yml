name: Release
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  synchronize:
    name: Synchronize files in `template/`
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout `template-dev`
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          path: template-dev
          repository: dhbw-typst/oderso-template-dev
          # ensure that we checkout the `main` branch on `template-dev`
          # this is VERY important when testing changes to this action via a PR
          # branch on the `oderso-template-dev` repository
          # if this is not specified, the checkout action will check out the PR
          # branch and will sync that to the specified (test) repository
          ref: main
      - name: Check for changes in template
        id: check_changes
        working-directory: template-dev
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q '^template/'; then
            echo "template_changed=true" >> $GITHUB_OUTPUT
          else
            echo "template_changed=false" >> $GITHUB_OUTPUT
          fi
      - name: Stop if nothing changed in template
        if: steps.check_changes.outputs.template_changed == 'false'
        run: |
          echo "No changes in template detected. Skipping sync and release."
      - name: Checkout `template`
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        if: steps.check_changes.outputs.template_changed == 'true'
        with:
          fetch-depth: 0
          path: template
          repository: dhbw-typst/oderso-template
          token: ${{ secrets.BOT_PAT }}
      - name: Install `git-filter-repo`
        if: steps.check_changes.outputs.template_changed == 'true'
        working-directory: /tmp
        run: |
          curl -L https://github.com/newren/git-filter-repo/releases/download/v2.47.0/git-filter-repo-2.47.0.tar.xz -o git-filter-repo.tar.xz
          tar -xvf git-filter-repo.tar.xz

          /tmp/git-filter-repo*/git-filter-repo --version
      - name: Set git user
        if: steps.check_changes.outputs.template_changed == 'true'
        run: |
          git config --global user.name "dhbw-typst-automation[bot]"
          git config --global user.email "dhbw-typst-automation[bot] <279977597+dhbw-typst-automation@users.noreply.github.com>"
      - name: Synchronize template files
        if: steps.check_changes.outputs.template_changed == 'true'
        run: /tmp/git-filter-repo*/git-filter-repo --source ./template-dev --target ./template --subdirectory-filter template/
      - name: Push changes
        if: steps.check_changes.outputs.template_changed == 'true'
        working-directory: template
        run: |
          git push --force

  prepare_release:
    name: Prepare release metadata
    needs: synchronize
    runs-on: ubuntu-24.04
    outputs:
      hasNextVersion: ${{ steps.version.outputs.hasNextVersion }}
      version: ${{ steps.version.outputs.version }}
      pr_number: ${{ steps.find_pr.outputs.pr_number }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
      - name: Get next version
        uses: thenativeweb/get-next-version@067dc4602577f4f61a51c3f4664552283a228c60
        id: version
        with:
          prefix: "v"
      - name: Find PR associated with the merge commit
        id: find_pr
        if: steps.version.outputs.hasNextVersion == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Find the PR associated with the current commit on `main`.
          pr_number=$(gh api \
            "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            --jq '.[0].number')

          if [[ -z "$pr_number" || "$pr_number" == "null" ]]; then
            echo "::error::Could not determine the PR number for commit ${{ github.sha }}."
            exit 1
          fi

          echo "Found PR #${pr_number} for commit ${{ github.sha }}"
          echo "pr_number=${pr_number}" >> "$GITHUB_OUTPUT"

  extract_release_notes:
    name: Extract release notes
    needs: prepare_release
    if: needs.prepare_release.outputs.hasNextVersion == 'true'
    uses: ./.github/workflows/extractReleaseNotes.yml
    with:
      pr_number: ${{ needs.prepare_release.outputs.pr_number }}
      fail_if_missing: true

  bump_version:
    name: Bump version, regenerate thumbnail and tag
    needs:
      - prepare_release
    if: needs.prepare_release.outputs.hasNextVersion == 'true'
    runs-on: ubuntu-24.04
    outputs:
      commit_sha: ${{ steps.commit.outputs.sha }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          token: ${{ secrets.BOT_PAT }}
      - name: Install typst
        uses: typst-community/setup-typst@63ac138db421d586de61f7f5ac3bcef6a2e6c78c # v5.1.0
        with:
          typst-version: 0.15.0
      - name: Set git user
        run: |
          git config user.name "dhbw-typst-automation[bot]"
          git config user.email "279977597+dhbw-typst-automation@users.noreply.github.com"
      - name: Bump version in typst.toml
        run: |
          # Strip the leading "v" from the tag (typst.toml uses bare semver).
          VERSION="${{ needs.prepare_release.outputs.version }}"
          VERSION_STRIPPED="${VERSION#v}"
          echo "Bumping typst.toml to ${VERSION_STRIPPED}"
          sed -i -E "s/^version = \".*\"$/version = \"${VERSION_STRIPPED}\"/" typst.toml
          grep '^version' typst.toml
      - name: Regenerate thumbnail
        run: typst compile template/main-dhbw-ka.typ -f png --pages 2 thumbnail.png
      - name: Commit, tag and push version bump
        id: commit
        run: |
          VERSION="${{ needs.prepare_release.outputs.version }}"
          git add typst.toml thumbnail.png
          # If there are no changes (e.g. version was already bumped manually)
          # we still want the tag to be created on the current HEAD.
          if ! git diff --cached --quiet; then
            git commit -m "chore: bump version to ${VERSION}"
          else
            echo "No version/thumbnail changes to commit."
          fi
          git tag "${VERSION}"
          git push origin HEAD:main
          git push origin "${VERSION}"
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  release:
    name: Create Release in Dev and Prod
    needs:
      - prepare_release
      - extract_release_notes
      - bump_version
    if: needs.prepare_release.outputs.hasNextVersion == 'true'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ needs.bump_version.outputs.commit_sha }}
          token: ${{ secrets.BOT_PAT }}
      - name: Install typst
        uses: typst-community/setup-typst@63ac138db421d586de61f7f5ac3bcef6a2e6c78c # v5.1.0
        with:
          typst-version: 0.15.0
      - name: Typst version
        run: typst --version
      - name: Build template
        run: |
          for file in $(ls template/main-*.typ); do
            typst compile $file
          done
      - name: Build documentation
        run: |
          typst compile documentation.typ --input version=${{ needs.prepare_release.outputs.version }}
      - name: Release dev repo
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          token: ${{ secrets.BOT_PAT }}
          files: |
            template/main-*.pdf
            documentation.pdf
          tag_name: ${{ needs.prepare_release.outputs.version }}
          body: ${{ needs.extract_release_notes.outputs.release_notes }}
      - name: Release prod repo
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          token: ${{ secrets.BOT_PAT }}
          files: |
            template/main-*.pdf
            documentation.pdf
          tag_name: ${{ needs.prepare_release.outputs.version }}
          repository: dhbw-typst/oderso-template
          body: ${{ needs.extract_release_notes.outputs.release_notes }}
